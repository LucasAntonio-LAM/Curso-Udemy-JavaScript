//                0        1         2        3          4
const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Júlia']
// POP
// const removidos = nomes.splice(-1, 1);

// SHIFT
// const removidos = nomes.splice(0, 1);

// PUSH
// nomes.splice(nomes.length, 0, 'Lucas', 'Antonio');

// UNSHIFT
// nomes.splice(0, 0, 'Lucas', 'Antonio')

// const removidos = nomes.splice(3, 2, 'Lucas', 'Marcos'); // Remover elementos do indice, primeiro o indice que vc quer começar a remover e depois o numero de indices que vc quer remover a partir daquele.
// // nomes.splice(indice, delete, el1, el2, el3);
console.log(nomes);